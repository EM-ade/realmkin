/**
 * Kingdom Game Layout — minimal wrapper, nav is hidden by GlobalNavigation/GlobalMobileNavigation themselves.
 */

export default function KingdomLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
